//рендерит строку поиска с кнопкой, хранит состояние с введенными данными

const MainInput = () => {
  return (
    <form>
      <div>
        <input type="search" />
        <input type="submit" />
      </div>
    </form>
  );
};

export default MainInput;
